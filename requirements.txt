accelerate==1.9.0
bitsandbytes==0.49.2
pydantic>=2.5,<3.0
torch==2.9.1 # If torch version is changed, also update in docker/rocm-silogen-finetuning-worker.Dockerfile
transformers[tokenizers]==4.57.3  # If transformers version is changed, also update in docker/rocm-silogen-finetuning-base.Dockerfile
kernels==0.12.0
datasets==3.2.0
trl==0.13.0
triton==3.5.1 # If triton version is changed, also update in docker/rocm-silogen-finetuning-worker.Dockerfile
mlflow==2.21.3
peft==0.16.0
ruamel.yaml>0.15.0,<1.0
sentencepiece>=0.1.0,<1.0.0
